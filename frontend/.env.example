# Frontend API base URL (production / GitHub Pages). Leave empty in dev to use Vite proxy (/api -> :8000).
# Production: HTTPS API origin only — no trailing slash, do not append /api or /api/payments.
# VITE_API_URL=https://germanursingpathway.onrender.com
